upstream frontend {
    server frontend:8080;
}

upstream api {
    server api:3000;
}

server {
    location / {
        proxy_pass http://frontend;
    }

    location /api {
        rewrite /api/(.*) /$1 break;
        proxy_pass http://api;
    }
}